# 领域约定

沉浸演艺合规控制台围绕演出版本、双轨审查、现场降级和调查裁决保存业务事实。主要对象为演出版本、场次、内容意见、安全意见、放行单、降级方案、红线事件、调查案。

必须长期遵守以下约束：内容与安全分别通过后才可放行；降级只能选预批方案；红线事件冻结关联版本；调查决定以追加方式保留历史。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。