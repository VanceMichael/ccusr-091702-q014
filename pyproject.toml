[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "show-compliance"
version = "0.1.0"
description = "沉浸演艺合规控制台后端服务"
requires-python = ">=3.12"

[tool.setuptools]
py-modules = ["app", "migrate"]